<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ultimate ATS | Pro Analyzer</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @keyframes float { 0% { transform: translateY(0px); } 50% { transform: translateY(-20px); } 100% { transform: translateY(0px); } }
        @keyframes blob { 0% { transform: translate(0px, 0px) scale(1); } 33% { transform: translate(30px, -50px) scale(1.1); } 66% { transform: translate(-20px, 20px) scale(0.9); } 100% { transform: translate(0px, 0px) scale(1); } }
        
        .blob-one { animation: blob 7s infinite; }
        .blob-two { animation: blob 6s infinite; }
        .blob-three { animation: blob 8s infinite; }
        
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: #020617; }
        ::-webkit-scrollbar-thumb { background: #334155; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: #475569; }
        
        /* Glow Effects */
        .glow-box-orange { box-shadow: 0 0 20px rgba(249, 115, 22, 0.15), inset 0 0 10px rgba(249, 115, 22, 0.05); }
        .glow-box-blue { box-shadow: 0 0 20px rgba(59, 130, 246, 0.15), inset 0 0 10px rgba(59, 130, 246, 0.05); }
        .glow-box-violet { box-shadow: 0 0 20px rgba(139, 92, 246, 0.15), inset 0 0 10px rgba(139, 92, 246, 0.05); }
        
        .glass { background: rgba(255, 255, 255, 0.03); backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px); border: 1px solid rgba(255, 255, 255, 0.08); }
        .glass-strong { background: rgba(15, 23, 42, 0.6); backdrop-filter: blur(20px); border: 1px solid rgba(255, 255, 255, 0.1); }
    </style>
</head>
<body class="bg-slate-950 text-white font-sans antialiased overflow-x-hidden selection:bg-orange-500 selection:text-white">

    <!-- Ambient Background -->
    <div class="fixed inset-0 overflow-hidden -z-10 pointer-events-none">
        <div class="blob-one absolute top-0 -left-4 w-96 h-96 bg-orange-500 rounded-full mix-blend-multiply filter blur-3xl opacity-20"></div>
        <div class="blob-two absolute top-0 -right-4 w-96 h-96 bg-blue-500 rounded-full mix-blend-multiply filter blur-3xl opacity-20"></div>
        <div class="blob-three absolute -bottom-32 left-20 w-96 h-96 bg-violet-500 rounded-full mix-blend-multiply filter blur-3xl opacity-20"></div>
    </div>

    <div class="max-w-7xl mx-auto px-4 py-12 relative z-10 flex flex-col gap-16">
        
        <!-- Header -->
        <div class="text-center space-y-4 animate-[float_6s_ease-in-out_infinite]">
            <div class="inline-block px-4 py-1 rounded-full border border-indigo-500/30 bg-indigo-500/10 text-indigo-300 text-xs font-bold tracking-widest uppercase">
                v6.0 Ultimate Engine
            </div>
            <h1 class="text-7xl md:text-9xl font-black tracking-tight text-transparent bg-clip-text bg-gradient-to-br from-orange-400 via-blue-400 to-violet-600 drop-shadow-2xl">
                ProATS
            </h1>
            <p class="text-xl text-slate-400 font-light tracking-wide max-w-2xl mx-auto">
                The world's most advanced resume analysis algorithm. 
                <span class="text-orange-400 font-bold">100% Clear Reports.</span>
            </p>
        </div>

        <!-- Input Grid -->
        <div class="glass rounded-3xl p-1 shadow-[0_0_50px_-12px_rgba(249,115,22,0.2)]">
            <div class="glass-strong rounded-[20px] p-8 md:p-12">
                <form method="POST" enctype="multipart/form-data" class="space-y-8">
                    
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                        <div class="group relative">
                            <input type="text" name="job_title" required placeholder=" " 
                                class="peer w-full bg-slate-800/50 border border-slate-700 text-xl text-white rounded-xl px-6 py-4 focus:outline-none focus:border-orange-500 focus:ring-1 focus:ring-orange-500 transition-all placeholder-transparent" />
                            <label class="absolute left-6 -top-2.5 px-2 bg-slate-900 text-orange-400 text-sm font-bold transition-all peer-placeholder-shown:text-xl peer-placeholder-shown:text-slate-500 peer-placeholder-shown:top-4 peer-focus:-top-2.5 peer-focus:text-orange-400 peer-focus:text-sm cursor-text">
                                Target Job Title
                            </label>
                        </div>
                        
                        <div class="relative h-16 group">
                            <input type="file" name="pdf_files" webkitdirectory directory multiple required
                                class="absolute inset-0 w-full h-full opacity-0 cursor-pointer z-20" />
                            <div class="w-full h-full bg-slate-800/50 border border-slate-700 border-dashed rounded-xl flex items-center justify-center gap-3 text-slate-400 transition-colors group-hover:border-violet-500 group-hover:text-white">
                                <i class="fa-solid fa-folder-open text-2xl"></i>
                                <span class="font-medium">Select Resume Folder</span>
                            </div>
                        </div>
                    </div>

                    <div class="group relative">
                        <textarea name="job_description" rows="5" required placeholder=" "
                            class="peer w-full bg-slate-800/50 border border-slate-700 text-lg text-white rounded-xl px-6 py-4 focus:outline-none focus:border-blue-500 focus:ring-1 focus:ring-blue-500 transition-all placeholder-transparent resize-none"></textarea>
                        <label class="absolute left-6 -top-2.5 px-2 bg-slate-900 text-blue-400 text-sm font-bold transition-all peer-placeholder-shown:text-xl peer-placeholder-shown:text-slate-500 peer-placeholder-shown:top-4 peer-focus:-top-2.5 peer-focus:text-blue-400 peer-focus:text-sm cursor-text">
                            Paste Job Description (Skills, Requirements, Role Overview)
                        </label>
                    </div>

                    <button type="submit" class="group relative w-full py-5 rounded-xl bg-gradient-to-r from-orange-600 to-blue-600 hover:from-orange-500 hover:to-blue-500 text-xl font-black text-white transition-all transform hover:scale-[1.01] hover:shadow-[0_0_30px_rgba(249,115,22,0.4)] overflow-hidden">
                        <span class="relative z-10 flex items-center justify-center gap-3">
                            <i class="fa-solid fa-bolt"></i> ANALYZE RESUMES
                        </span>
                        <div class="absolute inset-0 bg-white/20 -translate-x-full group-hover:translate-x-0 transition-transform duration-500"></div>
                    </button>

                    {% if error %}
                        <div class="p-4 rounded-xl bg-red-500/10 border border-red-500/30 text-red-400 text-center font-bold tracking-wide">
                            {{ error }}
                        </div>
                    {% endif %}
                </form>
            </div>
        </div>

        <!-- Results Display -->
        {% if results %}
            <div class="space-y-16">
                <div class="flex items-center justify-between border-b border-white/10 pb-4">
                    <h2 class="text-4xl font-bold text-white">Analysis Results</h2>
                    <span class="text-slate-400">{{ results|length }} Resumes Processed</span>
                </div>

                {% for item in results %}
                    <!-- Main Card -->
                    <div class="glass rounded-3xl overflow-hidden hover:shadow-[0_0_50px_-10px_rgba(249,115,22,0.2)] transition-all duration-500 border-t-4 border-t-transparent hover:border-t-orange-500">
                        
                        <!-- Card Header -->
                        <div class="p-8 border-b border-white/5 bg-white/5 flex flex-col md:flex-row justify-between items-start md:items-center gap-6">
                            <div class="flex items-center gap-6">
                                <div class="w-20 h-20 rounded-2xl bg-gradient-to-br from-slate-800 to-black border border-slate-700 flex items-center justify-center shadow-inner">
                                    <i class="fa-solid fa-file-pdf text-3xl text-slate-400"></i>
                                </div>
                                <div>
                                    <h3 class="text-3xl font-bold text-white tracking-tight">{{ item.filename }}</h3>
                                    <div class="flex gap-4 mt-2 text-sm text-slate-400 font-mono">
                                        <span><i class="fa-regular fa-clock"></i> {{ item.time }}s</span>
                                        <span><i class="fa-solid fa-layer-group"></i> {{ item.sections_detected|length }} Sections</span>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="flex flex-col items-center min-w-[150px]">
                                <div class="text-sm font-bold text-slate-500 uppercase tracking-wider mb-2">Match Score</div>
                                <div class="text-7xl font-black text-transparent bg-clip-text bg-gradient-to-r from-orange-400 to-blue-400 drop-shadow-lg">
                                    {{ item.match_score }}%
                                </div>
                            </div>
                        </div>

                        <!-- Body Grid -->
                        <div class="p-8 grid grid-cols-1 lg:grid-cols-12 gap-12">
                            
                            <!-- Left: ATS Breakdown & Improvements (7 Cols) -->
                            <div class="lg:col-span-7 space-y-8">
                                <div>
                                    <h4 class="text-xl font-bold text-white mb-6 flex items-center gap-3">
                                        <div class="w-2 h-8 bg-orange-500 rounded-full"></div>
                                        ATS Quality Score: {{ item.ats_score }}/100
                                    </h4>
                                    
                                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                        {% for log in item.ats_breakdown %}
                                            {% set label_val = log.split(': ') %}
                                            {% set val_raw = label_val[1] %}
                                            {% set score_parts = val_raw.split('/') %}
                                            {% set numerator = score_parts[0]|int %}
                                            {% set denominator = score_parts[1]|int %}
                                            {% set percent_calc = (numerator / denominator * 100) %}
                                            
                                            {% if percent_calc >= 80 %}
                                                {% set text_col = "text-orange-400" %}
                                                {% set bg_col = "bg-orange-500" %}
                                            {% elif percent_calc >= 60 %}
                                                {% set text_col = "text-blue-400" %}
                                                {% set bg_col = "bg-blue-500" %}
                                            {% elif percent_calc >= 40 %}
                                                {% set text_col = "text-violet-400" %}
                                                {% set bg_col = "bg-violet-500" %}
                                            {% else %}
                                                {% set text_col = "text-indigo-400" %}
                                                {% set bg_col = "bg-indigo-500" %}
                                            {% endif %}
                                            
                                            <div class="bg-white/5 rounded-xl p-4 border border-white/5 hover:border-white/10 transition-colors">
                                                <div class="flex justify-between text-sm mb-1">
                                                    <span class="text-slate-300 font-medium">{{ label_val[0] }}</span>
                                                    <span class="font-bold {{ text_col }}">{{ val_raw }}</span>
                                                </div>
                                                <div class="w-full bg-slate-800 rounded-full h-2 overflow-hidden">
                                                    <div class="h-full {{ bg_col }}" style="width: {{ percent_calc }}%"></div>
                                                </div>
                                            </div>
                                        {% endfor %}
                                    </div>
                                </div>

                                <!-- IMPROVEMENTS & MISSING REPORT -->
                                <div class="glass-strong rounded-xl p-6 border border-white/10 glow-box-violet">
                                    <h4 class="text-xl font-bold text-violet-400 mb-4 flex items-center gap-2">
                                        <i class="fa-solid fa-clipboard-check"></i> Clear Improvement Report
                                    </h4>
                                    
                                    <div class="space-y-6">
                                        <!-- Missing Keywords -->
                                        <div>
                                            <h5 class="text-sm font-bold text-red-400 uppercase tracking-wider mb-2">Missing Keywords (From JD)</h5>
                                            <div class="flex flex-wrap gap-2">
                                                {% if item.missing_keywords %}
                                                    {% for kw in item.missing_keywords %}
                                                    <span class="px-2 py-1 rounded-lg bg-red-500/10 text-red-300 border border-red-500/30 text-xs font-medium">
                                                        {{ kw }}
                                                    </span>
                                                    {% endfor %}
                                                {% else %}
                                                    <span class="text-slate-500 text-xs italic">No critical keywords missing.</span>
                                                {% endif %}
                                            </div>
                                        </div>

                                        <!-- Suggestions -->
                                        <div>
                                            <h5 class="text-sm font-bold text-yellow-400 uppercase tracking-wider mb-2">Improvements Needed</h5>
                                            <ul class="space-y-2 text-sm text-slate-300">
                                                {% for imp in item.improvements %}
                                                <li class="flex items-start gap-2">
                                                    <i class="fa-solid fa-triangle-exclamation text-yellow-500 mt-1"></i>
                                                    <span>{{ imp }}</span>
                                                </li>
                                                {% endfor %}
                                            </ul>
                                        </div>
                                    </div>
                                </div>
                                
                                <!-- RAW EXTRACTED TEXT SCROLL BOX -->
                                <div>
                                    <h5 class="text-sm font-bold text-slate-400 uppercase tracking-wider mb-2">Extracted Raw Text (Scrollable)</h5>
                                    <div class="h-40 bg-black rounded-lg border border-slate-700 p-4 overflow-y-auto font-mono text-xs text-slate-400 custom-scrollbar">
                                        {{ item.raw_text }}
                                    </div>
                                </div>

                            </div>

                            <!-- Right: Suggested Roles & Keywords (5 Cols) -->
                            <div class="lg:col-span-5 space-y-8">
                                
                                <!-- SCROLLABLE SUGGESTED ROLES BOX -->
                                <div class="glass-strong rounded-xl border border-blue-500/30 glow-box-blue h-[400px] flex flex-col">
                                    <div class="p-4 border-b border-white/5 bg-white/5">
                                        <h4 class="text-xl font-bold text-blue-400 flex items-center gap-2">
                                            <i class="fa-solid fa-wand-magic-sparkles"></i> Suggested Roles
                                        </h4>
                                    </div>
                                    <div class="p-4 overflow-y-auto flex-1 custom-scrollbar space-y-3">
                                        {% for role in item.suggested_roles %}
                                            <div class="group bg-white/5 border border-white/5 rounded-xl p-4 hover:bg-white/10 transition-all cursor-default">
                                                <div class="flex justify-between items-center mb-1">
                                                    <span class="font-bold text-white">{{ role.role }}</span>
                                                    <span class="bg-blue-500/20 text-blue-300 text-xs font-bold px-2 py-1 rounded-md">{{ role.score }}%</span>
                                                </div>
                                                <div class="text-xs text-slate-400">{{ role.rationale }}</div>
                                            </div>
                                        {% endfor %}
                                    </div>
                                </div>

                                <!-- Keywords Cloud -->
                                <div>
                                    <h4 class="text-sm font-bold text-slate-400 uppercase tracking-wider mb-3">Matched Keywords</h4>
                                    <div class="flex flex-wrap gap-2">
                                        {% for kw in item.keywords %}
                                        <span class="px-3 py-1.5 rounded-lg bg-orange-500/10 text-orange-300 border border-orange-500/20 text-sm font-medium">
                                            {{ kw }}
                                        </span>
                                        {% endfor %}
                                    </div>
                                </div>
                                
                                <!-- Detected Sections Summary -->
                                <div>
                                    <h4 class="text-sm font-bold text-slate-400 uppercase tracking-wider mb-3">Detected Sections</h4>
                                    <div class="flex flex-wrap gap-2">
                                        {% for sec in item.sections_detected %}
                                        <span class="px-3 py-1 rounded-lg bg-emerald-500/10 text-emerald-300 border border-emerald-500/20 text-xs font-bold uppercase">
                                            {{ sec }}
                                        </span>
                                        {% endfor %}
                                    </div>
                                </div>

                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </div>
</body>
</html>